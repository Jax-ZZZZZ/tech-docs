<%- include('layout', { body: `
<div class="col-md-3 sidebar" id="sidebar">
  ${Object.values(docs).map(group => `
    <div class="menu-group">
      <div class="menu-cat-title" onclick="toggle(this)">
        ${(typeof group.name === 'object' ? (group.name[lang] || group.name.zh) : group.name)}
      </div>

      <ul class="menu-items">
        ${group.children.map(cat => `
          ${cat.items.map(i => `
            <li>
              <a
                href="/?doc=${i.id}&lang=${lang}"
                class="${currentDoc && currentDoc.id === i.id ? 'active' : ''}"
              >
                ${(typeof i.title === 'object' ? (i.title[lang] || i.title.zh) : i.title)}
              </a>
            </li>
          `).join('')}
        `).join('')}
      </ul>
    </div>
  `).join('')}
</div>

<div class="sidebar-backdrop" id="sidebarBackdrop"></div>

<div class="col-md-9">
  <div class="content">
    ${htmlContent ? htmlContent : '<p>welcome to Tech Docs</p>'}
  </div>
</div>
` }) %>
