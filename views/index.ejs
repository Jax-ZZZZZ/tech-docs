<%- include('layout', { body: `
<div class="col-md-3 sidebar" id="sidebar">

  <!-- 搜索无结果提示 -->
  <div id="sidebarSearchHint" class="sidebar-search-hint" style="display:none;">
    <span>🔍</span>
    <span class="hint-text">
      ${lang === 'en' ? 'No matching documents' : '未找到匹配的文档'}
    </span>
  </div>

  ${Object.values(docs).map(group => `
    <div class="menu-group">
      <div class="menu-cat-title" onclick="toggle(this)" role="button" tabindex="0">
        ${(typeof group.name === 'object' ? (group.name[lang] || group.name.zh) : group.name)}
      </div>

      <ul class="menu-items">
        ${group.children.map(cat => `
          ${cat.items.map(i => `
            <li>
              <a
                href="/?doc=${i.id}&lang=${lang}"
                data-doc="${i.id}"
                class="${currentDoc && currentDoc.id === i.id ? 'active' : ''}"
              >
                ${(typeof i.title === 'object' ? (i.title[lang] || i.title.zh) : i.title)}
              </a>
            </li>
          `).join('')}
        `).join('')}
      </ul>
    </div>
  `).join('')}

</div>

<div class="sidebar-backdrop" id="sidebarBackdrop"></div>

<div class="col-md-9">
  <div class="content">
    ${
      htmlContent
        ? htmlContent
        : `
          <div class="empty-state">
            <p class="empty-text desktop-only">
              ${lang === 'en'
                ? 'Welcome to the Beyond Menu POS Help Center. Please select a document to get started.'
                : '欢迎来到Beyond Menu POS操作指南，请选择一个文档开始查看。'}
            </p>
            <p class="empty-text mobile-only">
              ${lang === 'en'
                ? 'Welcome! Tap “Help Center” above to explore the Beyond Menu POS guides.'
                : '点击上方「操作指南」开始查看Beyond Menu POS操作指南。'}
            </p>
          </div>
        `
    }
  </div>
</div>
` }) %>
